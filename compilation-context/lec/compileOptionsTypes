@types
typescript2.0 부터 사용 가능해진 내장 type definition시스템

아무설정 안하면?
 node_modules/@types라는 모든 경로를 찾아서 사용

typeRoots를 사용하면?
 배열 안에 들어 있는 경로들 아래서만 가져온다.

types를 사용하면
 배열 안의 모듈 혹은 ./node_modules/@types 안의 모듈에서 찾아온다.
 [] 빈 배열을 넣는다는건 이 시스템을 이용하지 않겠다는 것

typeRoots와 types를 같이 사용하지 않는다.

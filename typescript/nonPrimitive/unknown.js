"use strict";
// const aNumber: number = maybe;
if (maybe === true) {
    var aBoolean = maybe;
    // const aString: string = maybe;
}
// typeof type guard
if (typeof maybe === 'string') {
    var aString = maybe;
}
if (typeof maybe === 'boolean') {
    var aBoolean = maybe;
}
